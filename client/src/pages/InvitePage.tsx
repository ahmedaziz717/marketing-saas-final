import DashboardLayout from "@/components/DashboardLayout"; import { Button } from "@/components/ui/button"; import { trpc } from "@/lib/trpc"; import { CheckCircle2 } from "lucide-react"; import { toast } from "sonner"; import { useLocation,useRoute } from "wouter";
export default function InvitePage(){const[,params]=useRoute("/invite/:token");const[,setLocation]=useLocation();const accept=trpc.workspace.acceptInvite.useMutation({onSuccess:()=>setLocation("/app"),onError:e=>toast.error(e.message)});return <DashboardLayout><div className="mx-auto max-w-xl py-24"><div className="surface p-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-primary/10 text-primary"><CheckCircle2 className="h-6 w-6"/></div><h1 className="mt-6 font-editorial text-5xl">Join the workspace</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">Accept the invitation to enter EvokeLoop with the role selected by your teammate.</p><Button className="mt-7 rounded-full px-6" disabled={!params?.token||accept.isPending} onClick={()=>params?.token&&accept.mutate({token:params.token})}>Accept invitation</Button></div></div></DashboardLayout>}

