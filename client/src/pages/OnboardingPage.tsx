import { useState } from "react";
import { useLocation } from "wouter";
import { ArrowRight, Check, Copy, Globe2, Sparkles, UserPlus } from "lucide-react";
import { toast } from "sonner";
import { WebsiteImportWizard } from "@/components/WebsiteImportWizard";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";

export default function OnboardingPage() {
  const utils = trpc.useUtils(); const [, setLocation] = useLocation(); const [step, setStep] = useState(1); const [organizationId, setOrganizationId] = useState<number | null>(null); const [workspaceName, setWorkspaceName] = useState(""); const [inviteEmail, setInviteEmail] = useState(""); const [inviteRole, setInviteRole] = useState<"admin" | "creator" | "reviewer" | "publisher">("creator");
  const createWorkspace = trpc.workspace.create.useMutation(); const createInvite = trpc.workspace.createInvite.useMutation();
  const create = async () => { try { const result = await createWorkspace.mutateAsync({ name: workspaceName }); setOrganizationId(result.organizationId); setStep(2); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not create workspace"); } };
  const finish = async () => { await utils.workspace.mine.invalidate(); toast.success("Your workspace is ready"); setLocation("/app/catalog"); };
  const inviteAndFinish = async () => { if (!organizationId || !inviteEmail) return; try { const result = await createInvite.mutateAsync({ organizationId, email: inviteEmail, role: inviteRole, origin: window.location.origin }); await navigator.clipboard.writeText(result.inviteUrl); toast.success("Invite link copied"); await finish(); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not create invitation"); } };
  const labels = ["Organization and owner role", "Website, brand, and product catalog", "Invite a role-bound teammate"];
  return <div className="mx-auto max-w-6xl py-8 md:py-14"><div className="mb-10 flex items-center justify-between"><div><p className="eyebrow">Workspace setup</p><h1 className="mt-3 font-editorial text-5xl leading-none md:text-7xl">Build your creative frame.</h1></div><div className="hidden items-center gap-2 sm:flex">{[1,2,3].map(number => <div key={number} className={`grid h-9 w-9 place-items-center rounded-full text-sm font-semibold ${step >= number ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground"}`}>{step > number ? <Check className="h-4 w-4"/> : number}</div>)}</div></div><div className="surface grid overflow-hidden xl:grid-cols-[.62fr_1.38fr]"><aside className="bg-[#201c25] p-8 text-white md:p-10"><Sparkles className="h-5 w-5 text-violet-300"/><h2 className="mt-8 font-editorial text-4xl leading-none">Start with the company’s public truth.</h2><p className="mt-5 text-sm leading-6 text-white/55">EvokeLoop turns the website into an editable brand kit and governed product catalog before creative work begins.</p><div className="mt-12 space-y-5 text-sm">{labels.map((label,index) => <div key={label} className="flex gap-3"><span className={`grid h-5 w-5 place-items-center rounded-full text-[10px] ${step > index ? "bg-emerald-400 text-emerald-950" : "bg-white/10 text-white/60"}`}>{index+1}</span><span className="text-white/70">{label}</span></div>)}</div></aside><div className="p-7 md:p-10 lg:p-12">{step===1 && <div><p className="eyebrow">Step 1 of 3</p><h3 className="mt-3 text-2xl font-semibold">Name your organization</h3><p className="mt-2 text-sm text-muted-foreground">You begin as the workspace owner. Website discovery comes next.</p><div className="mt-8"><Label>Workspace name</Label><Input className="mt-2 h-12" value={workspaceName} onChange={event => setWorkspaceName(event.target.value)} placeholder="Acme Creative Studio"/></div><Button className="mt-8 h-12 rounded-full px-6" onClick={create} disabled={workspaceName.trim().length<2 || createWorkspace.isPending}>Continue to website import<Globe2 className="ml-2 h-4 w-4"/></Button></div>}{step===2 && organizationId && <div><p className="eyebrow mb-5">Step 2 of 3</p><WebsiteImportWizard organizationId={organizationId} onComplete={() => setStep(3)}/></div>}{step===3 && <div><p className="eyebrow">Step 3 of 3</p><h3 className="mt-3 text-2xl font-semibold">Define the first team role</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Invite a teammate now, or finish and manage roles later.</p><div className="mt-7 grid gap-5 sm:grid-cols-[1fr_180px]"><div><Label>Teammate email</Label><Input className="mt-2" type="email" value={inviteEmail} onChange={event => setInviteEmail(event.target.value)} placeholder="teammate@company.com"/></div><div><Label>Role</Label><Select value={inviteRole} onValueChange={value => setInviteRole(value as typeof inviteRole)}><SelectTrigger className="mt-2"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="admin">Admin</SelectItem><SelectItem value="creator">Creator</SelectItem><SelectItem value="reviewer">Reviewer</SelectItem><SelectItem value="publisher">Publisher</SelectItem></SelectContent></Select></div></div><div className="mt-7 flex flex-wrap gap-3"><Button onClick={inviteAndFinish} disabled={!inviteEmail || createInvite.isPending} className="rounded-full"><UserPlus className="mr-2 h-4 w-4"/>Invite & finish</Button><Button variant="outline" onClick={finish} className="rounded-full">Finish workspace<ArrowRight className="ml-2 h-4 w-4"/></Button></div><div className="mt-6 flex items-center gap-2 rounded-2xl bg-muted/60 p-4 text-xs text-muted-foreground"><Copy className="h-4 w-4 text-primary"/>Invite links are copied to your clipboard and expire after seven days.</div></div>}</div></div></div>;
}
