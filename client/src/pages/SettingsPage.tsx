import { useState } from "react"; import { WorkspaceGate } from "@/components/WorkspaceGate"; import { PageHeader } from "@/components/PageHeader"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select"; import { useWorkspace } from "@/hooks/useWorkspace"; import { trpc } from "@/lib/trpc"; import { UserPlus } from "lucide-react"; import { toast } from "sonner";
function Settings(){const{organizationId,organization,membership}=useWorkspace();const members=trpc.workspace.members.useQuery({organizationId:organizationId!},{enabled:!!organizationId});const[email,setEmail]=useState("");const[role,setRole]=useState<"admin"|"creator"|"reviewer"|"publisher">("creator");const invite=trpc.workspace.createInvite.useMutation({onSuccess:async data=>{await navigator.clipboard.writeText(data.inviteUrl);toast.success("Invite link copied")},onError:e=>toast.error(e.message)});return <><PageHeader eyebrow="Workspace administration" title="Settings & team" description="Manage the people and roles that can create, review, approve, and publish for this organization."/><div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><section className="surface p-6"><p className="eyebrow">Members</p><div className="mt-5 space-y-2">{members.data?.map(item=><div key={item.membership.id} className="flex items-center gap-3 rounded-2xl bg-muted/55 p-4"><div className="grid h-9 w-9 place-items-center rounded-full bg-card text-sm font-semibold">{item.user.name?.[0]??"U"}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.user.name??item.user.email}</p><p className="truncate text-xs text-muted-foreground">{item.user.email}</p></div><span className="rounded-full border hairline bg-card px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider">{item.membership.role}</span></div>)}</div></section><aside className="surface p-6"><p className="eyebrow">Invite teammate</p><h3 className="mt-3 text-xl font-semibold">Share a role-bound link</h3><div className="mt-6 space-y-4"><div><Label>Email</Label><Input className="mt-2" value={email} onChange={e=>setEmail(e.target.value)}/></div><div><Label>Role</Label><Select value={role} onValueChange={v=>setRole(v as typeof role)}><SelectTrigger className="mt-2"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="admin">Admin</SelectItem><SelectItem value="creator">Creator</SelectItem><SelectItem value="reviewer">Reviewer</SelectItem><SelectItem value="publisher">Publisher</SelectItem></SelectContent></Select></div><Button className="w-full" disabled={!email||invite.isPending} onClick={()=>invite.mutate({organizationId:organizationId!,email,role,origin:window.location.origin})}><UserPlus className="mr-2 h-4 w-4"/>Create & copy invite</Button></div><div className="mt-6 rounded-2xl bg-muted/60 p-4 text-xs leading-5 text-muted-foreground"><strong className="text-foreground">{organization?.name}</strong><br/>Your current role is {membership?.role}. Invite links expire after seven days.</div></aside></div></>}
export default function SettingsPage(){return <WorkspaceGate><Settings/></WorkspaceGate>}
